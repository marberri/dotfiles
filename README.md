# Dotfiles
Just a few things I think are worth holding on to. I try to keep things as simple as I can get away with.

## Usage (GNU Stow)

Create symlinks from `/dotfiles`:
```bash
stow -t /home/marberri/.config .
```
